This directory contains the scripts/data from the investigation into the bias of
the eigenvalue when calculated with an inexact (or noisy) Arnoldi's method.  The
noise comes from the inexact calculation of the matrix-vector product at each
Arnoldi iteration.  
